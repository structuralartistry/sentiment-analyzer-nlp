import org.scalatest.{FunSpec, Matchers}

class GenderAnalyzerSpec extends FunSpec with Matchers {

  describe("gender analyzer") {

    describe("pronouns") {

      it("should return MALE when input has a male pronoun") {
      }

      it("should return FEMALE when input has a female pronoun") {
      }

      it("should return INCONCLUSIVE when input has no pronouns") {
      }

    }

    describe("names") {

      it("should return MALE when input has a male name") {
      }

      it("should return FEMALE when input has a female name") {
      }

      it("should return INCONCLUSIVE when input has no names") {
      }

    }

  }

}
